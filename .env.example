# Chromaprint plus AcoustID identify audio that carries no usable tags, which is
# the last step in the cover art chain. Register an application at
# https://acoustid.org/new-application to get a client key. Without one the app
# skips that step and behaves exactly as it did before.
MAIN_VITE_ACOUSTID_KEY=
